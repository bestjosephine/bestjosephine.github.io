// Auto-generated by build.js — DO NOT EDIT
window.searchIndex = [
  {
    "title": "并查集模板",
    "url": "/posts/union-find-template/",
    "date": "2026-05-02",
    "dateDisplay": "May 2, 2026",
    "tag": "leetcode",
    "summary": "我已经30岁了，还要管并查集的事。",
    "body": "我已经30岁了，还要管并查集的事。 ```java /**  * 并查集（Union-Find）实现，支持按秩合并、路径压缩，并维护连通分量个数  * 使用 parent 数组存储每个节点的父节点，rank 数组存储树的秩（高度上界）  * count 记录当前图中连通分量的数量  */ public class UnionFind {     // parent[i] 表示节点 i 的父节点，根节点的父节点指向自身     private int[] parent;          // rank[i] 表示以 i 为根节点的树的高度（上界），用于按秩合并优化     private int[] rank;          // count 记录当前连通分量的个数，初始为 n，每次成功合并减1     private int count;     /**      * 构造一个包含 n 个独立元素的并查集      * @param n 元素个数，元素编号为 0 ~ n-1      */     public UnionFind(int n) {         parent = new int[n];         rank = new int[n];         count = n;  // 初始时有 n 个连通分量（每个元素独立）         for (int i = 0; i < n; i++) {             parent[i] = i;  // 每个元素自成一个集合，父节点指向自己             rank[i] = 0;    // 初始树高度为 0         }     }     /**      * 查找元素 x 所属集合的根节点（代表元）      * 使用了路径压缩优化：在查找过程中将路径上的所有节点直接指向根节点      * @param x 元素编号      * @return x 所在集合的根节点      */     public int find(int x) {         if (parent[x] != x) {             parent[x] = find(parent[x]); // 路径压缩         }         return parent[x];     }     /**      * 合并元素 x 和元素 y 所在的集合      * 采用按秩合并优化：将秩较小的树连接到秩较大的树上，避免树高度增长      * 若合并成功（原本属于不同集合），连通分量个数 count 减 1      * @param x 元素 x      * @param y 元素 y      * @return 如果 x 和 y 原本属于不同集合且合并成功，返回 true；否则返回 false（已属于同一集合）      */     public boolean union(int x, int y) {         int rootX = find(x);         int rootY = find(y);                  // 如果根相同，说明已经属于同一集合，无需合并         if (rootX == rootY) {             return false;         }                  // 按秩合并：将秩较小的树合并到秩较大的树上         if (rank[rootX] < rank[rootY]) {             parent[rootX] = rootY;         } else if (rank[rootX] > rank[rootY]) {             parent[rootY] = rootX;         } else {             // 两棵树秩相等，任意合并，并增加新根的秩             parent[rootY] = rootX;             rank[rootX]++;         }                  // 合并成功，连通分量个数减 1         count--;         return true;     }     /**      * 判断元素 x 和元素 y 是否属于同一集合      * @param x 元素 x      * @param y 元素 y      * @return true 表示同一集合，false 表示不同集合      */     public boolean isConnected(int x, int y) {         return find(x) == find(y);     }     /**      * 获取元素 x 所在集合的根节点（与 find 相同）      * @param x 元素 x      * @return 根节点编号      */     public int getRoot(int x) {         return find(x);     }     /**      * 返回当前连通分量的个数      * @return 连通分量个数      */     public int getCount() {         return count;     } } ``` 路径压缩：在 find() 中将查找路径上的所有节点直接挂到根上，使树更扁平。 按秩合并：在 union() 中总是将矮树贴到高树上，保持树高为对数级别。"
  },
  {
    "title": "二叉树",
    "url": "/posts/binary-tree/",
    "date": "2026-05-01",
    "dateDisplay": "May 1, 2026",
    "tag": "leetcode",
    "summary": "我已经快30岁了，已经不想再管二叉树家的事了。",
    "body": "我已经快30岁了，已经不想再管二叉树家的事了。 ## 二叉树的遍历 ### 1. 递归 前序遍历：根节点在前。遍历方式是根→左→右。 中序遍历：根节点在中。遍历方式是左→根→右。 后序遍历：根节点在后。遍历方式是左→右→根。 ```java /**  * Definition for a binary tree node.  * public class TreeNode {  *     int val;  *     TreeNode left;  *     TreeNode right;  *     TreeNode() {}  *     TreeNode(int val) { this.val = val; }  *     TreeNode(int val, TreeNode left, TreeNode right) {  *         this.val = val;  *         this.left = left;  *         this.right = right;  *     }  * }  */   public void traversal(TreeNode node) {     // 边界条件处理 \t\tif (node == null) return; \t\t// ⭐前序 root 节点 \t\t// 递归左子节点 \t\ttraversal(node.left); \t\t// ⭐中序 root 节点 \t\t// 递归右子节点 \t\ttraversal(node.right); \t\t// ⭐后序 root 节点 } ``` ### 2. 迭代 迭代真的好难哦！ **前序遍历**：栈。将根节点push进去。循环中，取出栈最上边的节点，读取数字后再依次push右节点和左节点。 ```java public List<Integer> preorder(TreeNode root) {     List<Integer> res = new ArrayList<>();     if (root == null) return res;     Deque<TreeNode> stack = new ArrayDeque<>();     stack.push(root);     while (!stack.isEmpty()) {         TreeNode node = stack.pop();         res.add(node.val);         if (node.right != null) stack.push(node.right); // 先右后左，保证左先出         if (node.left != null) stack.push(node.left);     }     return res; } ``` **中序遍历**：栈+双循环。内循环-把节点存到栈中，直到节点不存在左节点。外循环-更新节点。 ```java public List<Integer> inorder(TreeNode root) {     List<Integer> res = new ArrayList<>();     Deque<TreeNode> stack = new ArrayDeque<>();     TreeNode cur = root;     while (cur != null || !stack.isEmpty()) {         while (cur != null) {             stack.push(cur);             cur = cur.left;         }         cur = stack.pop();         res.add(cur.val);         cur = cur.right;     }     return res; } ``` **后序遍历**：双栈法。后序遍历是前序遍历的反着来，即从栈的角度来看，是根→右→左。所以栈1就负责先遍历根节点，把根节点 push 进栈2。前序遍历想先打印左节点，需要先push右节点，再push左节点；后序遍历想先打印右节点，需要先push左节点，再push右节点。 ```java public List<Integer> postorder(TreeNode root) {     List<Integer> res = new ArrayList<>();     if (root == null) return res;     Deque<TreeNode> s1 = new ArrayDeque<>();     Deque<TreeNode> s2 = new ArrayDeque<>();     s1.push(root);     while (!s1.isEmpty()) {         TreeNode node = s1.pop();         s2.push(node); \t\t\t\t// 先push左节点，再push右节点         if (node.left != null) s1.push(node.left);         if (node.right != null) s1.push(node.right);     }     while (!s2.isEmpty()) {         res.add(s2.pop().val);     }     return res; } ```"
  },
  {
    "title": "A2A协议",
    "url": "/posts/a2a-protocal/",
    "date": "2026-04-30",
    "dateDisplay": "Apr 30, 2026",
    "tag": "A2A",
    "summary": "> Produced by Qoder",
    "body": "> Produced by Qoder ### A2A 是什么 A2A（Agent2Agent）是 Google 在 2025 年 4 月发布的一个开放协议，解决的问题是：**AI Agent 之间怎么互相合作**。 ### A2A 和 MCP 的关系 MCP 解决的是\"AI 怎么调用工具\"的问题——AI 是老板，工具是打工仔，老板说干嘛就干嘛。 但还有一种场景：**两个 AI Agent 需要合作完成一件事**。 比如你有一个\"招聘 Agent\"和一个\"日程 Agent\"，招聘 Agent 筛完简历后想安排面试，它需要跟日程 Agent 商量：\"这个候选人下周有空，你帮我找个会议室订个时间？\"日程 Agent 可能会回：\"下周三下午可以，但需要候选人确认。\" 这不是简单的\"调用一个函数\"，而是两个 Agent 之间的**协商、对话、来回沟通**。MCP 解决不了这个问题，A2A 就是来解决这个的。 ``` MCP：  AI  →  工具      （主仆关系，单次调用） A2A：  AI  ⇄  AI        （协作关系，多轮对话） ``` 一句话：**MCP 是 AI 和工具之间的协议，A2A 是 AI 和 AI 之间的协议。** 两者互补，不冲突。 ### A2A 的几个核心概念 只有四个东西需要记住： #### 1. Agent Card（智能体名片） 每个 Agent 对外发布一张\"名片\"，放在固定地址 `/.well-known/agent.json`，告诉别人\"我是谁、我能干什么\"。 ```json // 比如一个\"翻译Agent\"的名片 {   \"name\": \"翻译助手\",   \"description\": \"支持中英日韩互译\",   \"url\": \"https://translate-agent.example.com\",   \"skills\": [     { \"id\": \"translate\", \"name\": \"文本翻译\" },     { \"id\": \"proofread\", \"name\": \"语法校对\" }   ],   \"capabilities\": {     \"streaming\": true,     \"pushNotifications\": true   } } ``` 类比：就像你在钉钉上看别人的个人资料——姓名、部门、擅长什么。其他 Agent 看了这张名片就知道\"哦，这个 Agent 能帮我翻译\"。 #### 2. Task（任务） A2A 里的核心交互单元是\"任务\"。一个 Agent 给另一个 Agent 派任务，任务有状态流转： ``` submitted（已提交）     ↓ working（处理中）     ↓ input-required（需要更多信息）  ← 可能来回几次     ↓ completed（完成）  或  failed（失败） ``` 跟 MCP 最大的区别在这里：MCP 调工具是**一次性的**——调一下，拿到结果，结束。A2A 的任务是**有状态的**，可能持续很长时间，中间可能需要多次沟通。 比如： ``` 招聘Agent → 日程Agent：\"帮我安排一场面试\"     （submitted） 日程Agent → 招聘Agent：\"候选人什么时候有空？\"  （input-required） 招聘Agent → 日程Agent：\"下周三或周四\"          （working） 日程Agent → 招聘Agent：\"已安排周三下午2点\"     （completed） ``` #### 3. Message（消息） Agent 之间通过消息沟通。每条消息有角色区分： - `user` — 客户端 Agent 发的 - `agent` — 服务端 Agent 发的 消息内容可以包含三种类型： - **TextPart**：纯文本 - **FilePart**：文件（图片、PDF 等） - **DataPart**：结构化 JSON 数据 所以 A2A 天然支持多模态——Agent 之间不仅能聊文字，还能互传文件和结构化数据。 #### 4. Artifact（产出物） 任务完成时，服务端 Agent 通过 Artifact 返回最终结果。可以是翻译好的文档、生成的图片、分析报告等。 ### 通信方式 A2A 基于 HTTP + JSON-RPC 2.0（跟 MCP 一样的消息格式），支持三种交互模式： ``` 1. 请求/响应    —— 简单场景，问一下答一下 2. SSE 流式    —— 任务处理中实时推送进度 3. Webhook 推送 —— 长时间任务完成后回调通知 ``` ### 一个完整的例子 假设你有一个\"旅行规划 Agent\"和一个\"机票 Agent\"： ``` 1. 旅行Agent 先看机票Agent 的名片（Agent Card）    GET https://flight-agent.com/.well-known/agent.json    → \"哦，它能搜航班、能订票\" 2. 旅行Agent 创建一个任务    POST https://flight-agent.com/a2a    {      \"method\": \"tasks/send\",      \"params\": {        \"id\": \"task-001\",        \"message\": {          \"role\": \"user\",          \"parts\": [{\"type\":\"text\", \"text\":\"搜一下5月1日北京到上海的航班\"}]        }      }    } 3. 机票Agent 处理中...可能需要更多信息    → 返回 status: \"input-required\"    → \"请问偏好经济舱还是商务舱？\" 4. 旅行Agent 补充信息    → \"经济舱，最便宜的\" 5. 机票Agent 完成任务    → status: \"completed\"    → artifact: [航班列表数据] ``` ### MCP vs A2A 对比 | | MCP | A2A | |---|---|---| | **解决什么** | AI 调用工具 | AI 和 AI 协作 | | **关系** | 主仆（Client 控制 Server） | 对等（两个 Agent 协商） | | **交互模式** | 无状态，调一次就完 | 有状态，多轮对话 | | **典型场景** | 查数据库、调 API、读文件 | 跨部门协作、委托任务、多 Agent 工作流 | | **发现机制** | 配置文件写死 | Agent Card，自动发现 | | **消息内容** | 主要是文本 | 文本 + 文件 + 结构化数据 | ### 一句话总结 **MCP 让 AI 能用工具，A2A 让 AI 能跟 AI 合作。** 未来的 AI 系统很可能两个都用——每个 Agent 通过 MCP 连接自己的工具，通过 A2A 跟其他 Agent 协同工作。"
  },
  {
    "title": "MCP是什么？",
    "url": "/posts/mcp/",
    "date": "2026-04-30",
    "dateDisplay": "Apr 30, 2026",
    "tag": "mcp",
    "summary": "> Produced by Qoder",
    "body": "> Produced by Qoder ## MCP 是什么？一个给实习生的入门指南 ### 先来一个比喻 想象你在餐厅吃饭： - **你（顾客）** = AI 助手（比如 QoderWork） - **菜单** = MCP 协议 - **厨房** = MCP Server（你写的程序） - **服务员** = 传输层（stdio / HTTP） 你不需要自己炒菜，你只需要看菜单点菜，服务员把你的需求传给厨房，厨房做好了再端给你。 MCP（Model Context Protocol）就是这个\"菜单+服务员\"的标准。它定义了 **AI 怎么发现和调用外部能力**。 --- ### 为什么需要 MCP？ 在没有 MCP 之前，如果你想让 AI 助手具备某种能力（比如查数据库、发消息、操作文件），你需要为每个 AI 平台单独写一套对接代码。 ``` 没有 MCP 的世界： 你的工具 ←→ 专门适配 ChatGPT 的代码 你的工具 ←→ 专门适配 Claude 的代码 你的工具 ←→ 专门适配 QoderWork 的代码 ... 每个平台都要写一遍 ``` 有了 MCP 之后： ``` 你的工具 ←→ MCP Server（写一次）←→ 任何支持 MCP 的 AI 客户端都能用 ``` 就像 USB 接口一样，统一标准之后，一根线到处用。 --- ### MCP Server 的三个核心概念 只有三个东西需要记住： #### 1. Tool（工具）—— 可以被调用的函数 最重要的概念。你可以把任何函数暴露为一个 Tool，AI 就能调用它。 ``` Tool 就像一个 API 接口： - 有名字（叫什么） - 有描述（做什么） - 有参数（需要什么输入） - 有返回值（产出什么） ``` #### 2. Resource（资源）—— 可以被读取的数据 类似于 GET 接口，提供一些数据让 AI 读取。比如配置信息、模板内容等。 #### 3. Transport（传输方式）—— Server 和 Client 怎么通信 目前主要有两种： - **stdio**：通过标准输入/输出通信（最常用，Client 把 Server 当子进程启动） - **HTTP SSE**：通过 HTTP 通信（适合远程部署） --- ### 逐行解释 server.js 下面把代码拆开，一行一行说清楚。 ```javascript // ========== 第一步：导入依赖 ========== // McpServer 是 MCP 的核心类，用来创建一个 Server import { McpServer } from \"@modelcontextprotocol/sdk/server/mcp.js\"; // StdioServerTransport 是 stdio 传输方式 // \"通过标准输入输出跟客户端通信\" import { StdioServerTransport } from \"@modelcontextprotocol/sdk/server/stdio.js\"; // zod 是一个参数校验库，用来定义\"工具接收什么参数\" import { z } from \"zod\"; ``` ```javascript // ========== 第二步：创建 Server 实例 ========== const server = new McpServer({   name: \"simple-hello-server\",  // 给你的 Server 起个名字   version: \"1.0.0\",             // 版本号 }); // 就像 Express 里的 const app = express() 一样 // 这一步只是\"创建\"，还没启动 ``` ```javascript // ========== 第三步：注册一个工具 ========== server.tool(   \"hello\",                    // 参数1：工具名称（AI 通过这个名字来调用）   \"向指定用户打招呼\",         // 参数2：工具描述（AI 通过这个来理解工具能干嘛）   {     name: z.string()          // 参数3：参数定义          .describe(\"用户的名字\")  // 这个工具需要一个字符串类型的 name 参数   },   async ({ name }) => {       // 参数4：执行函数（工具被调用时运行的代码）     return {       content: [         {           type: \"text\",           text: `你好，${name}！欢迎使用 MCP！`,         },       ],     };     // 返回值格式是固定的：content 数组，里面放 text 类型的内容   } ); // 类比：这就像在 Express 里写 //   app.post(\"/hello\", (req, res) => { ... }) // 只不过调用方不是浏览器，而是 AI ``` ```javascript // ========== 第四步：启动服务 ========== async function main() {   const transport = new StdioServerTransport();   // 创建一个 stdio 传输通道   await server.connect(transport);   // 把 Server 连接到这个传输通道上，开始监听请求   console.error(\"Server is running...\");   // 注意：用 console.error 而不是 console.log   // 因为 stdout（console.log）被 MCP 协议占用了   // stderr（console.error）才是给人看的日志 } main(); ``` --- ### 逐行解释 test-client.js ```javascript // ========== 客户端：模拟 AI 调用 MCP Server ========== // 导入客户端类 import { Client } from \"@modelcontextprotocol/sdk/client/index.js\"; import { StdioClientTransport } from \"@modelcontextprotocol/sdk/client/stdio.js\"; async function main() {   // 1. 创建传输层 —— 告诉客户端\"用 node server.js 启动 Server\"   const transport = new StdioClientTransport({     command: \"node\",      // 运行什么命令     args: [\"server.js\"],  // 命令参数   });   // 客户端会自动把 server.js 当子进程启动   // 然后通过 stdin/stdout 跟它通信   // 2. 创建客户端并连接   const client = new Client({ name: \"test-client\", version: \"1.0.0\" });   await client.connect(transport);   // 连接成功后，客户端和服务端就可以互相通信了   // 3. 发现工具 —— \"你有什么工具可以用？\"   const tools = await client.listTools();   // 返回 Server 注册的所有工具列表   // 4. 调用工具 —— \"帮我运行 hello 工具，参数是 name=张三\"   const result = await client.callTool({     name: \"hello\",               // 调用哪个工具     arguments: { name: \"张三\" }  // 传什么参数   });   // result.content[0].text === \"你好，张三！欢迎使用 MCP！\"   // 5. 读取资源   const resource = await client.readResource({ uri: \"greeting://template\" });   // 读取 Server 提供的资源数据 } ``` --- ### 实际使用场景中的数据流 当你在 QoderWork 中使用 MCP 时，完整流程是这样的： ``` 你说：\"帮我向张三打招呼\"         │         ▼    QoderWork（AI 客户端）    AI 想：\"我有一个 hello 工具可以用\"         │         ▼    QoderWork 调用 MCP Client    发送：{ method: \"tools/call\", params: { name: \"hello\", arguments: { name: \"张三\" } } }         │         ▼  （通过 stdio 传输）         │    你的 MCP Server（server.js）    收到请求 → 执行 hello 函数 → 返回结果         │         ▼    QoderWork 收到：\"你好，张三！欢迎使用 MCP！\"    AI 把结果组织成自然语言回复给你 ``` --- ### 如何接入到 QoderWork 写好 server.js 之后，你只需要在 QoderWork 的 MCP 配置中加一条： ```json {   \"mcpServers\": {     \"hello\": {       \"command\": \"node\",       \"args\": [\"/你的路径/mcp-simple-example/server.js\"]     }   } } ``` QoderWork 启动时会自动运行你的 Server，然后 AI 就能发现和使用你定义的工具了。 --- ### 小结 | 你写的东西 | 类比 | 作用 | |-----------|------|------| | `new McpServer()` | `express()` | 创建服务 | | `server.tool()` | `app.post()` | 注册一个可调用的接口 | | `server.resource()` | `app.get()` | 注册一个可读取的数据 | | `StdioServerTransport` | `app.listen(3000)` | 选择通信方式并启动 | | `zod` schema | Swagger 参数定义 | 告诉 AI 参数是什么类型 | 一句话总结：**MCP Server 就是给 AI 写的后端服务，Tool 就是 API 接口，Transport 就是监听方式。**"
  },
  {
    "title": "双轴排序",
    "url": "/posts/dual-pivot-quicksort/",
    "date": "2026-04-29",
    "dateDisplay": "Apr 29, 2026",
    "tag": "sort",
    "summary": "> Produced by DeepSeek",
    "body": "> Produced by DeepSeek ## 一、双轴排序的起源 ### 1.1 单轴快排：时代的经典与局限 快速排序是计算机科学中最重要的算法之一。其核心思想是选取一个“轴”（pivot），将数组划分为小于和大于轴的两个区域，然后递归排序。最坏情况发生在每次选取的轴恰好是数组中的最小值或最大值时，这导致递归树极不平衡，时间复杂度退化为 O(n²)。虽然可以通过随机采样等手段降低最坏情况的概率，但理论上无法完全消除。J. Bentley 和 D. McIlroy 在 1990 年代对快排进行了精妙优化，业界一度认为经典快排的潜力已被挖掘殆尽。 ### 1.2 双轴快排的诞生与发展 2009年，俄罗斯程序员 Vladimir Yaroslavskiy 在其业余时间实验中，发现了一种使用两个轴的双轴快排变体。起初学术界普遍认为多轴策略因分区复杂而得不偿失，但他的实测数据与这一成见截然相反，最终于2011年随 Java 7 发行而成为 `Arrays.sort()` 对原始类型数组的默认算法。Jonathan Bentley 和 Joshua Bloch 也共同参与了该算法在 Java 实现中的引入工作。 ## 二、核心原理与步骤 ### 2.1 算法思想概览 Double-Pivot QuickSort 的核心改进，是将原来的一次分为两个部分的“二分法”，升级为一次分为三个部分的“三分法”。算法从待排序数组中选取两个轴 `pivot1` 和 `pivot2`，并确保 `pivot1 ≤ pivot2`，当选取的两个元素不满足此条件时，算法会先交换它们。随后，通过一轮分区，将数组划分为三个区域： 1. **第一区域** (Region I)：所有元素 `< pivot1` 2. **第二区域** (Region II)：元素满足 `pivot1 ≤ x ≤ pivot2`（Java 的实现策略是对右侧的剩余区间依次扫描，一次性将每个元素分配到 L、R、G 三个区域之一） 3. **第三区域** (Region III)：所有元素 `> pivot2` 完成分区后，再递归地对三个子区间分别进行同样的双轴快排，直到整个数组有序。 ### 2.2 详细算法步骤 由于选取的两个轴（如最左和最右元素）可能是数组中极小或极大的值，导致三分区中的某个区域可能为空，无法充分发挥优势。因此，Yaroslavskiy 采用了**非对称扫描**策略：使用三个指针（`k` 扫描、`L` 指向 Region II 起点、`R` 指向 Region II 终点），一次性遍历并将元素归入相应区域，通过非对称划分技巧，有效避免了经典多轴分区中常见的额外开销。 <center> ### 双轴分区步骤 </center> | **步骤** | **操作** | **示例（单个元素）** | |:---|:---|:---| | **步骤 1** | 待处理元素 `A[k] < pivot1`（属于 Region I） | `A[k]` 与 `A[L]` 交换，`k++`, `L++` | | **步骤 2** | `A[k]` 介于 `pivot1` 和 `pivot2` 间（属于 Region II） | `k++`（仅移动指针，元素已在正确区间） | | **步骤 3** | `A[k] > pivot2`（属于 Region III） | 从 `R` 向左扫描，找到适合 Region II 或 Region I 的元素交换，`R--` | <sup>1. 本表基于 `k` 指针的移动策略</sup> 1. **预处理**：确保左轴 ≤ 右轴。 2. **初始化**：`L = left + 1`，`k = left + 1`，指向待扫描区域起点；`R = right - 1`，指向待扫描区域终点。 3. **分区循环**：当 `k ≤ R` 时循环。若元素小于 `pivot1`，将其交换至 Region I，`L++`, `k++`；若元素大于等于 `pivot2`，则激活对应处理过程；若元素在中间，直接 `k++`。 4. **最终调整**：遍历结束后，交换 `pivot1` 到 `L-1`，交换 `pivot2` 到 `R+1`。 ### 2.3 分区过程的直观示例 让我们通过一个具体例子来直观理解双轴分区。假设有一个待排序数组 `[5, 3, 8, 4, 2, 7, 1, 6]`，选择首尾元素作为双轴：`pivot1 = arr[left] = 5`，`pivot2 = arr[right] = 6`。 - **初始状态**：`L = 1`，`k = 1`，`R = 6`（索引位置）。 - **第一轮（k=1）**：`A[1] = 3 < 5`（Region I），与 `A[L]`（即 `A[1]` 自身）交换，状态不变，`L++` 变为 2，`k++` 变为 2。 - **第二轮（k=2）**：`A[2] = 8 > 6`（Region III），从 `R`（索引6）向左扫描找到小等于6的元素：`A[6] = 1 < 6`，于是交换 `A[2]`（8）和 `A[6]`（1），`R--` 变为 5。 - **第三轮（k=2）**：现在 `A[2] = 1 < 5`（Region I），与 `A[L]`（即 `A[2]` 自身）交换，`L++` 变为 3，`k++` 变为 3。 - **第四轮（k=3）**：`A[3] = 4 < 5`（Region I），与 `A[L]`（`A[3]=4`）交换，`L++` 变为 4，`k++` 变为 4。 - **第五轮（k=4）**：`A[4] = 2 < 5`（Region I），与 `A[L]`（`A[4]=2`）交换，`L++` 变为 5，`k++` 变为 5。 - **第六轮（k=5）**：`A[5] = 7 > 6`（Region III），从 `R`（索引5）向左扫描：`A[5] = 7 > 6`（继续向左），`R--` 变为 4，此时 `k=5 > R=4`，结束循环。 最终分区结果：`[3, 1, 4, 2, 5, 7, 8, 6]`，交换 `pivot1` 与 `A[L-1]`（即 `A[4] = 2`），交换 `pivot2` 与 `A[R+1]`（即 `A[6] = 8`），得到最终三分区： - Region I (<5)：`[3, 1, 4, 2]` - Region II (5~6)：`[5, 6]` - Region III (>6)：`[8, 7]` 递归排序各区域即可。 ## 三、性能分析与复杂度 ### 3.1 理论分析 经典的理论分析显示，在随机排列的大规模数据集上，双轴快排的平均比较次数约为 `1.9n ln n`，经典单轴快排为 `2n ln n`，比较次数减少了约 5%；然而双轴快排的平均交换次数约为 `0.6n ln n`，单轴为 `0.33n ln n`，交换次数几乎翻倍。 ### 3.2 “表象与事实”的深层解读 既然理论成本互有优劣，为何双轴快排在实践中总是胜出？**关键在于，现代计算机的性能瓶颈往往不在于比较和交换的“数量”，而在于算法对 CPU 缓存的使用效率**。更具体的机制包括： - **空间局部性**：Yaroslavskiy 算法的非对称扫描模式，使得对 Region I、II、III 的访问呈现出极大的**空间局部性**，对 CPU 缓存非常友好，减少了缓存未命中。虽然表面上做多了不少交换，实际这些交换的数据大多已经在缓存中，其成本比缓存未命中引起的停顿要低好几个数量级。 - **分支预测友好**：数据访问的模式化也降低了 CPU 分支预测失败的概率，进一步提升了指令流水线效率。 - **Java 字节码层面的反直觉**：一项 2013 年的学术研究发现，Yaroslavskiy 算法在 Java 字节码指令数量上甚至略多于经典快排，但其实际运行时间却更短。这有力地证明了性能的提升并非源于指令条数的减少，而是源于其在现代硬件架构上更优越的执行效率。 - **重复元素优化**：双轴快排能更自然地将重复元素归入中间分区，减少了递归深度。 ### 3.3 综合复杂度总结 尽管平均交换次数有所增加，但因其在现代硬件上优越的缓存和分支预测性能，双轴快排成为对原始类型数组排序的首选算法。以下是各算法性能对比： <center> ### 排序算法性能对比 </center> | **算法**      | **平均时间复杂度** | **额外空间**  | **稳定性** | **核心优势**                                           | |:--------------|:----------------|:------------|:----------|:-----------------------------------------------------| | 经典单轴快排     | O(n log n)       | O(log n) 递归 | 不稳定       | 实现简单                                                | | **双轴快排**    | O(n log n)       | O(log n) 递归 | **不稳定**    | 缓存友好，Java 7+ `Arrays.sort()` 对原始类型的 **默认算法** | | 三向切分       | O(n log n)       | O(log n) 递归 | 不稳定       | 重复元素优化良好，为现代多轴策略埋下伏笔                  | | 归并 / TimSort | O(n log n)       | O(n)         | **稳定**     | 稳定，链表友好                                          | <sup>1. 注：双轴快排和单轴快排都属于“不稳定”排序算法，即相同值的元素在排序后可能改变其先后次序。</sup> ### 3.4 为什么双轴快排如此之快？ 双轴快排的精髓是**以“更多的工作”换取“更好的工作”**。它看似做了更多比较和交换，但精心安排这些工作的顺序，让数据流更好地匹配现代 CPU 的缓存和流水线。当数据在缓存中停留更久、分支预测更准的时候，量变就引发了质变——总体的挂钟时间反而大幅缩短。 ## 四、实现及优化细节（以 Java 为例） ### 4.1 基础 Java 代码 Java 的 `Arrays.sort()` 对于原始类型数组，使用的是 `DualPivotQuicksort.sort()` 方法。以下是基于 JDK 核心逻辑的完整示例代码，展示了双轴快排的主控流程和分区逻辑： ```java public class DualPivotQuickSort {     // 对外公开的排序接口     public static void sort(int[] arr) {         if (arr == null || arr.length <= 1) return;         sort(arr, 0, arr.length - 1);     }     // 递归排序核心     private static void sort(int[] arr, int left, int right) {         if (left >= right) return;                     // 递归终止条件         // 1. 确保 arr[left] <= arr[right]         if (arr[left] > arr[right]) {             int tmp = arr[left];             arr[left] = arr[right];             arr[right] = tmp;         }         int pivot1 = arr[left];         int pivot2 = arr[right];         // 2. 初始化三个指针         int less = left + 1;      // Region I 区域边界         int great = right - 1;    // Region III 区域边界           int k = less;             // 当前扫描指针         // 3. 主分区循环         while (k <= great) {             if (arr[k] < pivot1) {                 swap(arr, k, less);                 less++;             } else if (arr[k] > pivot2) {                 while (k < great && arr[great] > pivot2) {                     great--;                 }                 swap(arr, k, great);                 great--;                 // 交换后，k 位置的新元素可能小于 pivot1，需要再次判断                 if (arr[k] < pivot1) {                     swap(arr, k, less);                     less++;                 }             }             k++;         }         // 4. 将两个轴放到正确位置         less--;         great++;         // 将 pivot1 放到 Region I 和 Region II 的分界         int tmp1 = arr[left];         arr[left] = arr[less];         arr[less] = tmp1;         // 将 pivot2 放到 Region II 和 Region III 的分界         int tmp2 = arr[right];         arr[right] = arr[great];         arr[great] = tmp2;         // 5. 递归处理三个区域         sort(arr, left, less - 1);      // Region I（小于 pivot1）         sort(arr, less + 1, great - 1); // Region II（介于两轴之间）         sort(arr, great + 1, right);    // Region III（大于 pivot2）     }     // 辅助交换函数     private static void swap(int[] arr, int i, int j) {         int temp = arr[i];         arr[i] = arr[j];         arr[j] = temp;     } } ``` ### 4.2 Java `Arrays.sort` 的宏策略 Java 的实现并非只使用双轴快速排序，而是根据不同情况选择最优算法： 1. **小数组（长度 < 47）**：使用**插入排序**。移除了递归带来的函数调用开销，对中小规模数据极为高效。 2. **中等数组或高度结构化数组**：使用**双轴快速排序**。其平均性能最好，具有自适应性。 3. **大数组且数据近乎有序（子序列过长）**：切换至**归并排序**。归并排序是稳定的，并在有序数据上表现优异。 4. **对象数组（如 Integer[]）**：默认使用 **TimSort** 算法，它是归并排序和插入排序的混合体，具备稳定性，并能利用输入数据中的“自然有序”特性，非常适合对象排序。 ## 五、总结 双轴快排的使用，标志着业界对算法效率的评估从“纯理论分析”进入了“软硬件协同优化”的新阶段，其核心思想是从“做更少的事”转变为“以更高效的方式做事”。`Arrays.sort` 宏策略的中文表述为：**小数组插排，混杂双轴快，高度有序切归并，对象用 TimSort**，这是 Java 生态中灵活运用多种算法优势的典型案例。下次当你调用 `Arrays.sort()` 处理数百万个整数时，都可以意识到，你正见证算法史上一个有趣的转折时刻。"
  },
  {
    "title": "力扣刷题技巧",
    "url": "/posts/leetcode-tricks/",
    "date": "2026-04-29",
    "dateDisplay": "Apr 29, 2026",
    "tag": "sort",
    "summary": "> Produced by DeepSeek95% and author5%",
    "body": "> Produced by DeepSeek(95%) and author(5%) 在 Java 中刷 LeetCode，掌握以下高频技巧可以显著提升编码效率。我将它们分为四大块：排序、类型转换、高级数据结构初始化、以及其他实用操作。 ## 求和 数组求和 ```java int sum = Arrays.stream(nums).sum(); ``` ## 一、排序技巧 ### 1. 数组排序 ```java int[] arr = {3, 1, 4}; Arrays.sort(arr);                 // 升序 [1,3,4] Arrays.sort(arr, 1, 3);           // 部分排序 [索引1到3) // 降序：需要包装类型 Integer[] boxed = {3,1,4}; Arrays.sort(boxed, (a,b)->b-a);   // 或 Comparator.reverseOrder() ``` 升序用到了双轴排序，而降序用到了 `Comparator`，所以需要包装类型。 Java 的 `Arrays.sort(int[])` 是专门为基本类型 `int` 设计的重载方法，它内部使用双轴快速排序，**只能按自然顺序（升序）排列**，不能接受自定义的 `Comparator`。 而排序降序需要提供一个 `Comparator`，例如 `(a,b) -> b - a`。`Comparator` 是一个泛型接口，其类型参数必须是**引用类型**（如 `Integer`、`String` 等），不能是基本类型（如 `int`）。因此，想要降序排序就必须使用引用类型的数组，即 `Integer[]`，然后调用 `Arrays.sort(T[], Comparator)` 方法。 简单来说：**Java 泛型不支持基本类型**，而 `Comparator` 依赖泛型，所以只能用包装类型。 如果实在不想用 `Integer[]`，也可以对 `int[]` 升序排序后，再手动反转数组。但这样会多一次遍历，并不比转成 `Integer[]` 更优。 ### 2. List排序 ```java List<Integer> list = Arrays.asList(3,1,4); Collections.sort(list);                          // 升序 list.sort((a,b)->a-b);                           // 同上 list.sort(Comparator.reverseOrder());            // 降序 list.sort((a,b)->b-a); ``` ### 3. 自定义对象排序 ```java // 按属性升序 Arrays.sort(persons, Comparator.comparing(Person::getAge)); // 多级排序：年龄升序，分数降序 Arrays.sort(persons, Comparator.comparing(Person::getAge)                                .thenComparing(Person::getScore, Comparator.reverseOrder())); // 注意：自定义比较器时，升序返回 o1 - o2，降序反之 ``` ### 4. 使用 lambda 快速实现 Comparator ```java // 按字符串长度排序 Arrays.sort(strs, (s1,s2) -> s1.length() - s2.length()); // 安全写法（防止溢出） Arrays.sort(strs, Comparator.comparingInt(String::length)); ``` ## 二、类型转换 ### 1. List <-> 数组 在 Java 中，`int[]` 和 `Integer[]` 的互转非常常见，主要利用流（Stream）或循环完成。 `int[]` → `Integer[]` ```java int[] primitiveArray = {1, 2, 3}; // 方法1：使用 Stream（推荐） Integer[] wrapperArray = Arrays.stream(primitiveArray)                               .boxed()                               .toArray(Integer[]::new); // 方法2：手动循环 Integer[] result = new Integer[primitiveArray.length]; for (int i = 0; i < primitiveArray.length; i++) {     result[i] = primitiveArray[i];  // 自动装箱 } ``` `Integer[]` → `int[]` ```java Integer[] wrapperArray = {1, 2, 3}; // 方法1：使用 Stream（推荐） int[] primitiveArray = Arrays.stream(wrapperArray)                              .mapToInt(Integer::intValue)                              .toArray(); // 方法2：手动循环 int[] result = new int[wrapperArray.length]; for (int i = 0; i < wrapperArray.length; i++) {     result[i] = wrapperArray[i];  // 自动拆箱 } ``` 若 `Integer[]` 中包含 `null`，自动拆箱会抛出 `NullPointerException`，需提前过滤。 对于 `long`/`Long`、`double`/`Double` 等，只需替换对应的映射方法：`mapToLong`、`mapToDouble` 等。 **List 转 int[]**（最常用，不需循环）： ```java List<Integer> list = new ArrayList<>(Arrays.asList(1,2,3)); // Java 8+ int[] arr = list.stream().mapToInt(i->i).toArray(); // 或使用 IntStream int[] arr = list.stream().mapToInt(Integer::intValue).toArray(); ``` **List 转 Integer[]**： ```java Integer[] boxed = list.toArray(new Integer[0]); ``` **int[] 转 List<Integer>**： ```java int[] arr = {1,2,3}; List<Integer> list = Arrays.stream(arr).boxed().collect(Collectors.toList()); ``` **String[] 转 List<String>**： ```java List<String> list = Arrays.asList(strArray);  // 固定长度 List<String> list = new ArrayList<>(Arrays.asList(strArray)); // 可变 ``` ### 2. Set <-> List / 数组 **Set 转 List**： ```java Set<Integer> set = new HashSet<>(Arrays.asList(1,2,3)); List<Integer> list = new ArrayList<>(set); ``` **Set 转 int[]**： ```java int[] arr = set.stream().mapToInt(i->i).toArray(); ``` **List 转 Set**： ```java Set<Integer> set = new HashSet<>(list); ``` ### 3. Map 的键/值转数组 ```java Map<String, Integer> map = new HashMap<>(); // Key 集合转数组 String[] keys = map.keySet().toArray(new String[0]); Integer[] values = map.values().toArray(new Integer[0]); // 转 int[]（需要 stream） int[] intValues = map.values().stream().mapToInt(i->i).toArray(); ``` ### 4. 字符与字符串互转 ```java String s = \"hello\"; char[] chars = s.toCharArray();               // 最常用 List<Character> list = s.chars().mapToObj(c->(char)c).collect(Collectors.toList()); // char[] 转 String String str = new String(chars); ``` ## 三、高级数据结构初始化 ### 1. 栈（Stack） ```java Deque<Integer> stack = new ArrayDeque<>();   // 推荐，比 Stack 快 stack.push(1); int top = stack.pop(); int peek = stack.peek(); ``` ### 2. 双端队列（Deque） ```java Deque<Integer> deque = new ArrayDeque<>(); // 当作队列使用 deque.offer(1);        // 尾部添加 int front = deque.poll();   // 头部弹出 // 双端操作 deque.offerFirst(0); deque.offerLast(2); ``` ### 3. 优先队列（堆） ```java // 最小堆（默认） PriorityQueue<Integer> minHeap = new PriorityQueue<>(); // 最大堆 PriorityQueue<Integer> maxHeap = new PriorityQueue<>((a,b)->b-a); // 或 Comparator.reverseOrder() // 自定义对象堆（按 score 最小） PriorityQueue<Node> heap = new PriorityQueue<>(Comparator.comparingInt(n -> n.score)); // 初始化时直接传入集合 PriorityQueue<Integer> heap = new PriorityQueue<>(list); ``` ### 4. 单调栈常用套路 ```java Deque<Integer> stack = new ArrayDeque<>(); for (int i = 0; i < n; i++) {     // 维护单调递增栈：弹出比当前大的元素     while (!stack.isEmpty() && stack.peek() > nums[i]) {         // 处理栈顶         stack.pop();     }     stack.push(nums[i]); } ``` ### 5. 快速创建 List/Set/Map ```java // 固定长度 List List<Integer> list = Arrays.asList(1,2,3); // 可变 List List<Integer> list = new ArrayList<>(Arrays.asList(1,2,3)); // 初始化 Set Set<String> set = new HashSet<>(Arrays.asList(\"a\",\"b\")); // 初始化 Map（最多10个键值对，Java 9+） Map<String, Integer> map = Map.of(\"k1\",1, \"k2\",2);  // 不可变 Map<String, Integer> mutable = new HashMap<>(Map.of(\"k1\",1, \"k2\",2)); // 传统方式 Map<String, Integer> map = new HashMap<String, Integer>() {{     put(\"a\",1);     put(\"b\",2); }}; ``` ## 四、其他实用技巧 ### 1. 数组拷贝 ```java int[] copy = Arrays.copyOf(arr, arr.length);       // 完整拷贝 int[] part = Arrays.copyOfRange(arr, 1, 4);        // 索引1~3 int[] copy = arr.clone();                          // 一维数组可用 // 二维数组深拷贝：需要循环或流 ``` ### 2. 数组填充 ```java int[] arr = new int[10]; Arrays.fill(arr, -1);                   // 全部填充-1 Arrays.fill(arr, 2, 5, 0);              // 索引2~4填0 ``` ### 3. 字符串常用操作 ```java // 剔除首尾空格 String trimmed = s.trim(); // 分割字符串（注意转义） String[] parts = s.split(\",\"); // 字符串拼接（数组/List） String joined = String.join(\"-\", list);   // 列表元素用-连接 String joined = String.join(\",\", arr);    // 数组也行（需变参） // 字符重复（Java 11+） String bar = \"=\".repeat(10); ``` ### 4. 常用集合快速判空与取值 ```java // 获取 list 最后一个元素 int last = list.get(list.size()-1); // 安全获取 map 值，不存在返回默认 int val = map.getOrDefault(key, 0); // 计算并更新 map.put(key, map.getOrDefault(key, 0) + 1); ``` ### 5. 数组与列表逆序 ```java // 数组逆序（自己写循环或用 Collections 辅助） List<Integer> list = Arrays.asList(1,2,3); Collections.reverse(list); // 对 int[] 逆序需要先转 List 或手写循环 ``` ### 6. 两数/三数之和排序双指针模板 ```java Arrays.sort(arr); int left = 0, right = n-1; while (left < right) {     int sum = arr[left] + arr[right];     if (sum == target) {...}     else if (sum < target) left++;     else right--; } ``` ### 7. 快速打印调试 ```java System.out.println(Arrays.toString(arr)); System.out.println(list); System.out.println(Arrays.deepToString(2Darray)); ``` \t ## Map 相关的高级技巧 ### 一、Map 的初始化技巧 ```java // 1. 不可变单对（Java 9+） Map<String, Integer> map = Map.of(\"a\", 1);  // 最多10个键值对 // 2. 不可变多对 Map<String, Integer> map = Map.of(\"a\", 1, \"b\", 2, \"c\", 3); // 3. 使用 Map.ofEntries（无数量限制） Map<String, Integer> map = Map.ofEntries(     Map.entry(\"a\", 1),     Map.entry(\"b\", 2) ); // 4. 可变 Map（双括号初始化，注意内存泄漏风险，仅刷题可用） Map<String, Integer> map = new HashMap<String, Integer>() {{     put(\"a\", 1);     put(\"b\", 2); }}; // 5. 从另一个 Map 拷贝 Map<String, Integer> newMap = new HashMap<>(oldMap); ``` ### 二、Map 遍历与迭代 ```java Map<String, Integer> map = new HashMap<>(); // 1. 遍历键值对（最常用） for (Map.Entry<String, Integer> entry : map.entrySet()) {     String key = entry.getKey();     int value = entry.getValue(); } // 2. Java 8 Lambda map.forEach((key, value) -> System.out.println(key + \":\" + value)); // 3. 遍历所有键 for (String key : map.keySet()) { } // 4. 遍历所有值 for (int value : map.values()) { }  // 注意：values() 返回 Collection<Integer> ``` ### 三、常用方法速查 ```java // 安全获取值，不存在返回默认值 int val = map.getOrDefault(key, 0); // 如果 key 不存在则计算并放入，返回计算值（线程安全但刷题很少用） map.computeIfAbsent(key, k -> new ArrayList<>()).add(value); // 更新存在的键的值，不存在则不操作 map.computeIfPresent(key, (k, v) -> v + 1); // 通用的 compute：无论存在与否都计算 map.compute(key, (k, v) -> (v == null) ? 1 : v + 1); // 合并：如果 key 不存在则放入 defaultValue，否则用 remappingFunction 合并 map.merge(key, 1, Integer::sum);   // 计数常用 // 如果 key 不存在则放入，返回旧值或 null map.putIfAbsent(key, 1); // 替换（仅当 key 存在且当前 value 等于 oldValue 时） map.replace(key, oldVal, newVal); ``` ### 四、计数场景最佳实践 ```java // 方法1：getOrDefault + put map.put(key, map.getOrDefault(key, 0) + 1); // 方法2：merge（最简洁） map.merge(key, 1, Integer::sum); // 方法3：compute map.compute(key, (k, v) -> (v == null) ? 1 : v + 1); // 方法4：传统判断 int cnt = map.getOrDefault(key, 0); map.put(key, cnt + 1); ``` ### 五、Map 与 List/Set 转换 ```java // 1. Map 的 key 集合转 List List<String> keyList = new ArrayList<>(map.keySet()); // 2. Map 的 value 集合转 List List<Integer> valueList = new ArrayList<>(map.values()); // 3. Map 的 key 集合转 Set（本身就是 Set） Set<String> keySet = map.keySet();  // 注意：这个 Set 受 map 影响，修改会同步 // 4. Map 转 List<Entry> List<Map.Entry<String, Integer>> entries = new ArrayList<>(map.entrySet()); // 5. 根据 Map 构建新的 List（例如提取某些属性） List<String> result = map.entrySet().stream()     .filter(e -> e.getValue() > 0)     .map(Map.Entry::getKey)     .collect(Collectors.toList()); ``` ### 六、Map 排序 ```java // 1. 按 key 排序（TreeMap 自动排序） Map<String, Integer> sortedByKey = new TreeMap<>(map); // 2. 临时按 key 排序（得到 List 或 LinkedHashMap） map.entrySet().stream()    .sorted(Map.Entry.comparingByKey())    .collect(Collectors.toMap(        Map.Entry::getKey,         Map.Entry::getValue,        (e1, e2) -> e1,   // 解决冲突        LinkedHashMap::new    )); // 3. 按 value 排序（最常用） List<Map.Entry<String, Integer>> list = new ArrayList<>(map.entrySet()); list.sort(Map.Entry.comparingByValue());                 // 升序 list.sort(Map.Entry.comparingByValue(Comparator.reverseOrder())); // 降序 // 4. 按 value 排序后取前 K 个 List<String> topK = map.entrySet().stream()     .sorted(Map.Entry.<String, Integer>comparingByValue().reversed())     .limit(k)     .map(Map.Entry::getKey)     .collect(Collectors.toList()); ``` ### 七、数组 / List 转 Map ```java // 1. 两个数组转 Map（key 数组，value 数组） String[] keys = {\"a\",\"b\"}; Integer[] vals = {1,2}; Map<String, Integer> map = IntStream.range(0, keys.length)     .boxed()     .collect(Collectors.toMap(i -> keys[i], i -> vals[i])); // 2. List<Person> 按 id 作为 key 转 Map Map<Integer, Person> personMap = persons.stream()     .collect(Collectors.toMap(Person::getId, Function.identity())); // 3. 处理重复 key（例如取第一个或合并） Map<Integer, Person> map = persons.stream()     .collect(Collectors.toMap(Person::getId, Function.identity(), (old, newVal) -> old)); // 4. 按某个属性分组（List 转 Map<Integer, List<Person>>） Map<Integer, List<Person>> groupByAge = persons.stream()     .collect(Collectors.groupingBy(Person::getAge)); ``` ### 八、其他实用技巧 ```java // 判断两个 Map 相等（比较键值对） boolean equal = map1.equals(map2);  // 注意：顺序不影响，但需要值也 equal // 清空 Map map.clear(); // 从 Map 中移除条件 map.entrySet().removeIf(entry -> entry.getValue() == 0); // 只保留指定 key 集合中的项（若修改原 Map） map.keySet().retainAll(keysToKeep); // 合并两个 Map（若有相同 key，值相加） map2.forEach((k, v) -> map1.merge(k, v, Integer::sum)); // 获取最大 value 对应的 key String maxKey = map.entrySet().stream()     .max(Map.Entry.comparingByValue())     .map(Map.Entry::getKey)     .orElse(null); ``` ### 九、频率统计的快速写法 ```java // 统计字符频率 String s = \"leetcode\"; Map<Character, Integer> freq = new HashMap<>(); for (char c : s.toCharArray()) {     freq.merge(c, 1, Integer::sum); } // 统计单词频率 String[] words = {\"apple\",\"banana\",\"apple\"}; Map<String, Integer> map = new HashMap<>(); for (String w : words) map.merge(w, 1, Integer::sum); ``` ### 十、注意点 - `map.keySet()` 返回的 Set 受原 Map 影响，若修改（如删除）会同步到 Map，但**不能直接 add**。 - `map.values()` 同理，修改会影响原 Map。 - 对 `map.entrySet()` 迭代时，不能直接 `remove()`（会抛异常），需要使用迭代器的 `remove()` 或 `removeIf`。 - 使用 `HashMap` 时，如果已知大概容量，指定初始容量可以避免频繁 rehash，如 `new HashMap<>(n*2)`。 掌握这些 Map 技巧，刷题时处理频率、分组、缓存、排序等场景将非常顺手。 ## 总结建议 - 优先使用 `Deque<Integer> stack = new ArrayDeque<>();` 而不是 `Stack`。 - 排序比较器 `(a,b)->a-b` 只在确信不会溢出时使用；通用写法用 `Integer.compare(a,b)` 或 `Comparator`. - 转换 `int[]` 和 `List<Integer>` 时，流式操作简洁但微量性能损失，可以接受。 - 初始化常用 `Arrays.asList(...)` 配合 `new ArrayList<>` 得到可变列表。"
  },
  {
    "title": "Reflection和Critic机制的关系和区别",
    "url": "/posts/difference-reflection-critic/",
    "date": "2026-04-23",
    "dateDisplay": "Apr 23, 2026",
    "tag": "Reflection, Critic",
    "summary": "> Produced by Qwen-3.6-Plus",
    "body": "> Produced by Qwen-3.6-Plus Critic 和 Reflection 是**串行关系**，Critic 在前，Reflection 在后。可以理解为 Critic 是\"发现问题的眼睛\"，Reflection 是\"解决问题的大脑\"。 ### 具体流程 **第一步：Planner 生成** 主流程调用召回子流程获取知识、策略、工具、案例后，Planner 节点的 LLM 基于这些 context 生成一个初步回复或动作。 **第二步：Critic 检查** 小模型对 Planner 输出做两轮校验： - 幻觉校验 — 把生成内容与召回的知识/ISO 做对比，检查是否有编造成分 - 指令遵循校验 — 把生成内容与服务策略做对比，检查是否遵守了策略要求 Critic 输出一个判定结果：通过 / 不通过。如果通过，直接输出给用户，流程结束。 **第三步：Reflection 诊断（仅在 Critic 不通过时触发）** Reflection 拿到 Critic 的失败信号后，进一步分析问题的根因，区分两种情况： **情况 A：召回阶段已有正确答案，但 LLM 没用上。** 这说明是生成环节的问题。Reflection 会在 prompt 中显式标注被忽略的关键信息，加强约束后重新让 Planner 生成。这一步不需要重新召回，只需要重新生成。 **情况 B：召回阶段就没找到合适的答案。** 这说明是检索环节的问题。Reflection 会调整检索策略，比如换关键词、触发定向召回（从指定知识库精准查询），然后带着新的召回结果重新进入 Planner 生成。 **第四步：再次 Critic** 修正后的输出会再过一遍 Critic。如果仍然不通过，可以再循环一次，但为了控制 RT，通常设有最大重试次数（一般 1-2 次）。超过次数后走兜底逻辑（安全回复或转人工）。 ### 完整时序 ``` Planner 生成     ↓ Critic 检查 ──── 通过 ──→ 输出给用户     ↓ 不通过 Reflection 诊断     ↓     ├─ 情况A（有答案没用）→ 强化prompt → 重新生成 → 再次 Critic     │     └─ 情况B（没召回到）→ 调整检索策略 → 重新召回 → 重新生成 → 再次 Critic ``` 所以整个链路是严格串行的：生成 → 检查 → 诊断 → 修正 → 再检查。Reflection 不会在 Critic 之前或同时运行，因为它依赖 Critic 的判定结果来决定修正方向。"
  },
  {
    "title": "CI 自动构建测试",
    "url": "/posts/ci-test/",
    "date": "2026-04-21",
    "dateDisplay": "Apr 21, 2026",
    "tag": "Test",
    "summary": "这是一篇测试文章，用于验证 CI 能否自动构建 HTML 并提交回 main 分支。",
    "body": "这是一篇测试文章，用于验证 CI 能否自动构建 HTML 并提交回 main 分支。 如果你在网站上看到这篇文章，说明 CI 工作正常！"
  },
  {
    "title": "获取阿里云 DashScope API Key 教程",
    "url": "/posts/get-bailian-api-key/",
    "date": "2026-04-21",
    "dateDisplay": "Apr 21, 2026",
    "tag": "Model, API key",
    "summary": "步骤",
    "body": "### 步骤 1. **开通 DashScope**    - 打开 https://bailian.console.aliyun.com/    - 用阿里云账号登录（没有的话先注册，支持支付宝/钉钉登录）    - 首次使用需要开通\"百炼\"服务，按提示操作即可 2. **创建 API Key**    - 开通后，进入 https://bailian.console.aliyun.com/?apiKey=1#/api-key    - 点击\"创建新的 API-KEY\"    - 复制生成的 Key（格式类似 `sk-xxxxxxxxxxxxxxxxxxxxxxxx`）    - ⚠️ 只在创建时显示一次，记得保存好 3. **免费额度**    - 通义千问有免费额度，新用户每月有一定量的免费调用次数    - 具体额度见：https://help.aliyun.com/zh/model-studio/developer-reference/tongyi-qianwen 4. **可用的免费模型**    - `qwen-turbo`：速度快，适合日常使用（免费额度高）    - `qwen-plus`：能力强一些，也有免费额度    - `qwen-max`：最强模型，免费额度较少"
  },
  {
    "title": "记忆的分类：情景记忆、工具记忆、工作记忆",
    "url": "/posts/memory-distinguish/",
    "date": "2026-04-21",
    "dateDisplay": "Apr 21, 2026",
    "tag": "Memory",
    "summary": "> Produced by Qwen-3.6-Plus",
    "body": "> Produced by Qwen-3.6-Plus 这三种记忆类型是现代 **LLM-based AI Agent 认知架构（Cognitive Architecture）** 的核心组件，灵感来源于人类认知心理学，但针对大语言模型的上下文限制、工具调用需求和长程推理能力进行了工程化重构。下面逐一分解其定义、核心技术、设计思想及典型实现。 --- ### 📘 1. Episodic Memory（情景记忆） #### 🔍 定义与作用 - **心理学原型**：记录个人经历的具体事件（时间、地点、人物、结果），具有时序性和情境依赖性。 - **AI Agent 中的映射**：存储 Agent 与用户或环境的**历史交互轨迹**（对话、任务流、状态变化）。由于 LLM 上下文窗口有限，需通过**摘要压缩**将长程经验转化为可检索的紧凑表示，避免信息丢失同时节省 Token 开销。 #### 🛠 核心技术与思想 | 技术/思想 | 说明 | |-----------|------| | **递归摘要（Recursive Summarization）** | 将原始对话/日志分块 → LLM 生成局部摘要 → 多轮合并为全局摘要，支持“原始→摘要→元摘要”分层存储 | | **向量化检索（RAG + Embedding）** | 将摘要或关键事件编码为向量，存入 Milvus/Pinecone/FAISS 等向量库，按需语义检索 | | **时序索引与生命周期管理** | 按时间戳、会话 ID、任务类型建立索引；支持 TTL 过期、重要性加权、遗忘机制（模拟人类记忆衰减） | | **经验回放（Experience Replay）** | 类似强化学习，从历史轨迹中采样高价值片段注入 Prompt，提升少样本泛化能力 | #### 📦 典型实现 - `MemGPT` 的 Archival Memory + 自动压缩管道 - `LangChain` 的 `ConversationSummaryMemory` / `ConversationTokenBufferMemory` - 企业级 Agent 平台的“会话知识库”或“用户画像记忆体” --- ### 🛠 2. Tool Memory（工具记忆） #### 🔍 定义与作用 - **心理学原型**：近似“程序性记忆”（Procedural Memory），记录技能与操作模式。 - **AI Agent 中的映射**：专门记录**工具调用历史、参数配置、执行结果、成功/失败反馈**。用于：   - 避免重复调用或错误参数   - 构建工具使用先验（Few-shot Tool-Use）   - 支持调试、回溯与策略优化 #### 🛠 核心技术与思想 | 技术/思想 | 说明 | |-----------|------| | **执行轨迹日志（Execution Trace）** | 结构化记录 `tool_name → args → output → latency → status`，通常以 JSON/Graph 形式存储 | | **工具注册表与 Schema 缓存** | 维护工具描述、参数类型、返回值格式，支持动态发现与类型校验 | | **效果反馈与评分机制** | 记录用户显式反馈（✅/❌）或隐式信号（任务完成度、重试次数），用于工具排序或策略微调 | | **结果缓存与语义去重** | 对幂等工具（如搜索、查询）缓存结果；对相似调用进行语义聚类，减少冗余推理 | | **工具图谱（Tool Graph）** | 记录工具间的依赖/互斥关系（如 `先调用 A 获取 ID → 再调用 B`），支持自动编排 | #### 📦 典型实现 - `LangSmith` / `OpenTelemetry` 的 Trace 记录系统 - `AutoGen` 的 Tool Execution Logger - 自研 Agent 平台的 `ToolHistoryDB` + 向量相似度匹配缓存 --- ### ⚡ 3. Working Memory（工作记忆） #### 🔍 定义与作用 - **心理学原型**：短期记忆容量有限（约 7±2 个组块），负责当前任务的主动加工、推理与状态保持。 - **AI Agent 中的映射**：维持**当前活跃的推理状态、任务分解栈、中间变量、注意力焦点**。是 Agent 执行 `Thought → Action → Observation` 循环的“工作台”。 #### 🛠 核心技术与思想 | 技术/思想 | 说明 | |-----------|------| | **上下文窗口管理** | 滑动窗口、注意力掩码、关键信息置顶（System Prompt 固定区） | | **状态机/图结构（State Graph）** | 使用 `LangGraph`、`StateFlow` 等显式建模任务状态转移，支持分支、回滚、并发 | | **堆栈/队列管理（Stack/Queue）** | 任务分解为子任务压栈（DFS）或入队（BFS）；支持优先级调度与中断恢复 | | **推理轨迹显式化** | Chain-of-Thought (CoT)、Tree-of-Thoughts (ToT)、Self-Correction Loop 将中间步骤写入工作记忆 | | **动态 Prompt 组装** | 根据当前栈顶任务动态注入相关上下文、工具描述、约束条件，避免无关信息干扰 | #### 📦 典型实现 - `ReAct` / `Plan-and-Execute` 的 Thought-Action-Observation 循环缓冲区 - `LangGraph` 的 `State` 对象与 `Checkpointer` - 自研 Agent 的 `TaskStack` + `ActiveContextManager` --- ### 🔗 三者协同机制与设计哲学 | 维度 | 协同逻辑 | 核心设计思想 | |------|----------|--------------| | **数据流** | Working Memory 驱动当前推理 → 调用 Tool → 结果写入 Tool Memory → 关键经验压缩至 Episodic Memory | **分层抽象 + 按需检索**（避免上下文爆炸） | | **检索触发** | 当 Working Memory 遇到相似任务/工具调用时，从 Episodic/Tool Memory 拉取相关片段注入 Prompt | **Memory-Augmented Generation**（RAG + In-Context Learning） | | **更新策略** | 工作记忆随任务推进刷新；工具记忆按执行结果增量追加；情景记忆定期异步压缩 | **流式处理 + 异步归档**（解耦实时推理与长期存储） | | **容错与学习** | 失败调用写入 Tool Memory 并标记；成功轨迹压缩为情景记忆；工作记忆通过 CoT 自我修正 | **经验驱动的策略演化**（无需梯度更新即可“学习”） | --- ### 🌐 前沿趋势与挑战 1. **记忆粒度自适应**：动态决定“哪些信息该保留原始、哪些该压缩”，引入重要性评分模型（如基于信息增益或任务价值） 2. **跨模态/多智能体记忆共享**：多个 Agent 共用 Tool Memory 或 Episodic Memory，实现群体经验迁移 3. **记忆与微调的融合**：将高价值记忆自动转化为 SFT/RLHF 数据，实现“记忆→参数”的知识固化 4. **隐私与合规**：情景记忆需支持用户数据删除、脱敏、联邦存储，符合 GDPR/AI Act 要求 --- ### 💡 总结 | 记忆类型 | 核心职责 | 关键技术 | 类比人类认知 | |----------|----------|----------|----------------| | **Episodic Memory** | 长程经验存储与压缩 | 递归摘要、向量检索、生命周期管理 | 情景记忆 / 自传体记忆 | | **Tool Memory** | 工具调用历史与效果反馈 | 执行 Trace、工具图谱、结果缓存 | 程序性记忆 / 技能库 | | **Working Memory** | 当前推理状态与任务栈管理 | 状态图、堆栈调度、动态 Prompt | 工作记忆 / 注意力焦点 | 这套架构的本质是**用工程化手段模拟人类认知的“有限容量+长期经验+技能积累”机制**，在 LLM 的固定上下文窗口之外，构建可扩展、可检索、可演化的记忆系统，是迈向“自主持续运行 Agent”的关键基础设施。"
  },
  {
    "title": "大模型代码评审方案",
    "url": "/posts/model-review/",
    "date": "2026-04-21",
    "dateDisplay": "Apr 21, 2026",
    "tag": "Model",
    "summary": "",
    "body": "``` #!/usr/bin/env python3 \"\"\" 多模型代码互审脚本 使用阿里云 DashScope（通义千问）的两个模型互相 review 代码。 模型 A（Reviewer 1）和 模型 B（Reviewer 2）分别独立审查，然后交叉评审对方的意见。 使用方式：     python code_review.py <代码文件路径> 首次使用前：     1. 安装依赖：pip install openai     2. 设置 API Key（二选一）：        - 环境变量：export DASHSCOPE_API_KEY=\"sk-xxxx\"        - 或直接修改下方 CONFIG 中的 api_key \"\"\" import sys import os import argparse import json from datetime import datetime # ============================================================ #  配置区 —— 根据你的情况修改 # ============================================================ CONFIG = {     # DashScope API Key（优先使用环境变量 DASHSCOPE_API_KEY）     \"api_key\": \"\",     # DashScope 兼容 OpenAI 的接口地址     \"base_url\": \"https://dashscope.aliyuncs.com/compatible-mode/v1\",     # 两个模型配置：使用不同模型可以获得更多样化的审查视角     \"reviewer_1\": {         \"name\": \"审查员 A\",         \"model\": \"qwen3.6-plus\",    # 能力强，适合深度审查         \"temperature\": 0.3,     },     \"reviewer_2\": {         \"name\": \"审查员 B\",         \"model\": \"qwen3.6-plus\",    # 同模型不同温度，提供差异化视角         \"temperature\": 0.7,     },     # 互审轮次（建议 1-2 轮，太多轮容易重复）     \"review_rounds\": 1,     # 审查报告输出目录（空字符串表示输出到代码文件同目录）     \"output_dir\": \"\", } # ============================================================ #  Prompt 模板 # ============================================================ INITIAL_REVIEW_PROMPT = \"\"\"你是一位资深软件工程师，正在进行代码审查（Code Review）。 请对以下代码进行全面审查，从以下维度分析： 1. **正确性**：逻辑是否正确？有没有 bug 或边界条件遗漏？ 2. **安全性**：有没有安全漏洞？（如 SQL 注入、XSS、硬编码密钥等） 3. **性能**：有没有性能问题？算法复杂度是否合理？ 4. **可读性**：命名、结构、注释是否清晰？ 5. **可维护性**：代码是否易于修改和扩展？有没有重复代码？ 6. **最佳实践**：是否符合该语言的惯用写法和社区规范？ 请按优先级排序你的发现： - 🔴 严重问题（必须修复） - 🟡 建议改进（推荐修复） - 🟢 小建议（锦上添花） 对于每个问题，请给出： - 具体位置（行号或代码片段） - 问题描述 - 修改建议（最好附上改进后的代码示例） 最后给出一个整体评分（1-10 分）和总结。 --- 代码如下： ``` {code} ``` \"\"\" CROSS_REVIEW_PROMPT = \"\"\"你是一位资深软件工程师。另一位审查员已经对一段代码给出了 review 意见。 请你审查这些意见： 1. 这些意见是否正确？有没有误判的？ 2. 有没有被遗漏的重要问题？ 3. 建议的优先级排序是否合理？ 4. 修改建议是否合适？有没有更好的方案？ 请给出你的交叉评审意见，指出你同意和不同意的地方，并补充任何遗漏的问题。 --- 被审查的代码： ``` {code} ``` --- 另一位审查员的意见： {other_review} \"\"\" SUMMARY_PROMPT = \"\"\"你是一位技术负责人。两位审查员已经完成了代码审查和交叉评审。 请你综合他们的意见，生成一份最终的代码审查报告。 要求： 1. 合并去重两位审查员的发现 2. 给出最终的优先级排序 3. 对于有争议的问题，给出你的判断 4. 列出明确的 Action Items（待办事项） 5. 给出最终的整体评分（1-10 分） --- 被审查的代码： ``` {code} ``` --- 审查员 A 的初始审查： {review_a} --- 审查员 B 的初始审查： {review_b} --- 审查员 A 对 B 的交叉评审： {cross_a} --- 审查员 B 对 A 的交叉评审： {cross_b} \"\"\" # ============================================================ #  核心逻辑 # ============================================================ def get_api_key():     \"\"\"获取 API Key\"\"\"     key = os.environ.get(\"DASHSCOPE_API_KEY\", \"\") or CONFIG[\"api_key\"]     if not key:         print(\"❌ 错误：未设置 API Key！\")         print()         print(\"请通过以下方式之一设置：\")         print(\"  方式一：设置环境变量\")         print(\"    export DASHSCOPE_API_KEY=\\\"sk-xxxx\\\"\")         print()         print(\"  方式二：直接修改脚本中 CONFIG 的 api_key 字段\")         print()         print(\"  获取 API Key：https://bailian.console.aliyun.com/?apiKey=1#/api-key\")         sys.exit(1)     return key def call_model(api_key, model_config, messages):     \"\"\"调用模型 API（使用 OpenAI 兼容接口）\"\"\"     from openai import OpenAI     client = OpenAI(         api_key=api_key,         base_url=CONFIG[\"base_url\"],     )     response = client.chat.completions.create(         model=model_config[\"model\"],         messages=messages,         temperature=model_config.get(\"temperature\", 0.3),     )     return response.choices[0].message.content def read_code_file(file_path):     \"\"\"读取代码文件\"\"\"     if not os.path.exists(file_path):         print(f\"❌ 错误：文件不存在 - {file_path}\")         sys.exit(1)     with open(file_path, \"r\", encoding=\"utf-8\") as f:         code = f.read()     if not code.strip():         print(\"❌ 错误：文件内容为空\")         sys.exit(1)     return code def print_section(title, content):     \"\"\"格式化输出一个段落\"\"\"     border = \"=\" * 60     print(f\"\\n{border}\")     print(f\"  {title}\")     print(f\"{border}\\n\")     print(content)     print() def run_review(file_path):     \"\"\"执行完整的互审流程\"\"\"     api_key = get_api_key()     code = read_code_file(file_path)     file_name = os.path.basename(file_path)     r1 = CONFIG[\"reviewer_1\"]     r2 = CONFIG[\"reviewer_2\"]     print(f\"\\n🔍 开始审查文件：{file_name}\")     print(f\"   审查员 A：{r1['name']}（{r1['model']}）\")     print(f\"   审查员 B：{r2['name']}（{r2['model']}）\")     print(f\"   互审轮次：{CONFIG['review_rounds']}\")     # 收集所有结果用于生成报告     all_results = {         \"file\": file_name,         \"file_path\": os.path.abspath(file_path),         \"timestamp\": datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\"),     }     # ---- 第一步：两个模型分别独立审查 ----     print(f\"\\n⏳ 第 1 步：{r1['name']} 正在独立审查...\")     review_a = call_model(api_key, r1, [         {\"role\": \"user\", \"content\": INITIAL_REVIEW_PROMPT.format(code=code)}     ])     print_section(f\"{r1['name']} 的审查结果\", review_a)     all_results[\"review_a\"] = review_a     print(f\"⏳ 第 2 步：{r2['name']} 正在独立审查...\")     review_b = call_model(api_key, r2, [         {\"role\": \"user\", \"content\": INITIAL_REVIEW_PROMPT.format(code=code)}     ])     print_section(f\"{r2['name']} 的审查结果\", review_b)     all_results[\"review_b\"] = review_b     # ---- 第二步：交叉评审 ----     print(f\"⏳ 第 3 步：{r1['name']} 正在评审 {r2['name']} 的意见...\")     cross_a = call_model(api_key, r1, [         {\"role\": \"user\", \"content\": CROSS_REVIEW_PROMPT.format(             code=code, other_review=review_b         )}     ])     print_section(f\"{r1['name']} 对 {r2['name']} 的交叉评审\", cross_a)     all_results[\"cross_review_a\"] = cross_a     print(f\"⏳ 第 4 步：{r2['name']} 正在评审 {r1['name']} 的意见...\")     cross_b = call_model(api_key, r2, [         {\"role\": \"user\", \"content\": CROSS_REVIEW_PROMPT.format(             code=code, other_review=review_a         )}     ])     print_section(f\"{r2['name']} 对 {r1['name']} 的交叉评审\", cross_b)     all_results[\"cross_review_b\"] = cross_b     # ---- 第三步：生成综合报告 ----     print(\"⏳ 第 5 步：正在生成综合审查报告...\")     summary = call_model(api_key, r1, [         {\"role\": \"user\", \"content\": SUMMARY_PROMPT.format(             code=code,             review_a=review_a,             review_b=review_b,             cross_a=cross_a,             cross_b=cross_b,         )}     ])     print_section(\"📋 综合审查报告\", summary)     all_results[\"summary\"] = summary     # ---- 保存报告 ----     output_dir = CONFIG[\"output_dir\"] or os.path.dirname(os.path.abspath(file_path))     timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")     report_name = f\"review_{os.path.splitext(file_name)[0]}_{timestamp}.md\"     report_path = os.path.join(output_dir, report_name)     report_content = generate_markdown_report(all_results)     with open(report_path, \"w\", encoding=\"utf-8\") as f:         f.write(report_content)     print(f\"\\n✅ 审查完成！报告已保存到：{report_path}\") def generate_markdown_report(results):     \"\"\"生成 Markdown 格式的审查报告\"\"\"     r1 = CONFIG[\"reviewer_1\"]     r2 = CONFIG[\"reviewer_2\"]     report = f\"\"\"# 代码审查报告 - **文件**：`{results['file']}` - **路径**：`{results['file_path']}` - **时间**：{results['timestamp']} - **审查员 A**：{r1['name']}（{r1['model']}） - **审查员 B**：{r2['name']}（{r2['model']}） --- ## 1. {r1['name']} 的独立审查 {results['review_a']} --- ## 2. {r2['name']} 的独立审查 {results['review_b']} --- ## 3. {r1['name']} 对 {r2['name']} 的交叉评审 {results['cross_review_a']} --- ## 4. {r2['name']} 对 {r1['name']} 的交叉评审 {results['cross_review_b']} --- ## 5. 综合审查报告 {results['summary']} \"\"\"     return report # ============================================================ #  入口 # ============================================================ def main():     parser = argparse.ArgumentParser(         description=\"多模型代码互审工具 —— 使用通义千问双模型交叉审查代码\",         formatter_class=argparse.RawDescriptionHelpFormatter,         epilog=\"\"\" 示例：     python code_review.py main.py     python code_review.py src/utils.js     DASHSCOPE_API_KEY=\"sk-xxx\" python code_review.py app.py         \"\"\",     )     parser.add_argument(\"file\", help=\"要审查的代码文件路径\")     parser.add_argument(         \"--rounds\", type=int, default=None,         help=\"互审轮次（默认 1 轮）\",     )     args = parser.parse_args()     if args.rounds is not None:         CONFIG[\"review_rounds\"] = args.rounds     run_review(args.file) if __name__ == \"__main__\":     main() ```"
  },
  {
    "title": "技术方案模版 — 从需求到上线的完整框架",
    "url": "/posts/tech-design-template/",
    "date": "2026-04-20",
    "dateDisplay": "Apr 20, 2026",
    "tag": "Engineering",
    "summary": "一份完整的技术方案模版，涵盖业务背景、架构设计、风险分析、测试发布全流程。适用于中大型项目的技术评审和团队协作。",
    "body": "一份完整的技术方案模版，涵盖业务背景、架构设计、风险分析、测试发布全流程。适用于中大型项目的技术评审和团队协作。 ## changelog | 时间 | 修订人 | 修改内容 | | --- | --- | --- | |  |  |  | ## 1. 业务背景和目标 ### 相关链接 | 文档 | 链接 | 备注 | | --- | --- | --- | | MRD |  |  | | PRD |  |  | | 技术方案 |  |  | | AONE |  |  | | AB 实验 |  |  | | 实验白名单 |  |  | | 代码地址 |  |  | | 灰度方案 |  |  | | 异常处理 |  |  | | 效果观测 |  |  | | 测试报告 |  |  | ### 业务背景 ### 需求详情 ## 2. 技术方案 ### 2.1 技术选型 > 参考业界案例和技术选型进行对比分析，针对需求确定最终的技术方案可选择2+的方案进行对比和分析，最终确定整体架构设计以及设计内容 ### 2.2 架构设计 > 流程图、时序图、架构图等 ### 2.3 领域模型/数据库表结构/字段等 > 领域模型、ER图 ### 2.4 接口/数据协议 > 涉及到的关键信息，包括HSF接口协议、MTOP接口协议等、switch、diamond ### 2.5 数据埋点方案 > 明确录入需要采集的业务和技术指标，如埋点、监控、报表等，确保项目上线后有必要的数据支撑尽量避免先上线，后补数据的场景 ### 2.6 灰度方案 > 可选方案包括：beta、安全spe、业务白名单、人群圈选、百分比等，评估要包含以下几个维度：接口兼容性、数据兼容性、回滚策略 ## 3. 技术风险分析 ### 3.1 监控完整性和有效性 > 需要增加什么监控，报警如何配置 ### 3.2 流量模型分析 > 系统流量分析，包含业务流量模型变化、接口限流值调整、流量超限的业务影响、下游依赖影响，分日常态和大促态讨论 ### 3.3 外部依赖 > 依赖的外部服务不可用情况下的处理方案 ### 3.4 应急方案设计 > 涉及业务关键链路节点的代码及配置变更，具备1分钟发现、5分钟止血、10分钟修复/回滚的能力，方案包括开关控制、版本控制等动态变更方案 ### 3.5 资损风险分析 > 如果业务涉及到资金、权益等链路，需要完成资金安全评估，包含资金调用链路图、对账方案、资损评估 ## 4. 测试范围分析 > 测试重点、变更内容、实验、开关、冒烟测试记录 | 变更内容（应用/Agent/服务策略） | 分支/版本信息 | 环境信息 | | --- | --- | --- | | 应用 | 分支信息 | 主预发 | | 服务策略 | 版本信息 | 0.0.1 | ## 5. 发布计划 ### 5.1 发布时间和顺序 > 前后端的发布时间节奏、依赖关系、重点执行事项等 **发布前** **发布中** **发布后** ### 5.2 灰度放量 > 灰度方案，从哪里开始做实验分流，不同模块间实验是否需要对齐等 ### 5.3 回滚方案 ## 6. 项目成员及时间计划 | 项目成员 | 具体参与者 | | --- | --- | | 业务 |  | | 产品 |  | | 交互 |  | | 测试 |  | | 前端 |  | | 后端 |  | | 数据 |  | | 算法 |  | **模块工时评估** | 开发投入时间 |  | | --- | --- | | 联调时间 |  | | 提测时间 |  | | 发布时间 |  | | 灰度节奏 | Day1: 1%, Day2: 10%, Day3: 30%, Day4: 50%, Day5: 100% | ## 7. 后续问题复盘或业务结果 > 如果遇到发布后有问题的话，可在这里记录一下针对后续业务效果的总结"
  },
  {
    "title": "Hello World — Welcome to My Blog",
    "url": "/posts/hello-world/",
    "date": "2026-04-09",
    "dateDisplay": "Apr 9, 2026",
    "tag": "Getting Started",
    "summary": "Welcome to my new blog! This is the very first post — a simple Hello World to kick things off.",
    "body": "Welcome to my new blog! This is the very first post — a simple `Hello World` to kick things off. I created this space to share my thoughts and learnings around technology, artificial intelligence, software engineering, and various topics that spark my curiosity. The design is intentionally minimal and clean, inspired by blogs I admire — where the focus is on the content, not the decoration. ## What to Expect Here are some of the topics I plan to write about: - **AI & Machine Learning** — Deep dives into LLMs, reinforcement learning, and emerging research - **Software Engineering** — System design patterns, best practices, and lessons from production - **Open Source** — Projects I'm working on and tools I find useful - **Random Thoughts** — Books, ideas, and anything interesting ## Why a Static Blog? I chose to build this as a plain static site — no frameworks, no build tools, just HTML and CSS. There's something refreshing about that simplicity. It loads fast, it's easy to maintain, and it will still work perfectly in 10 years. > \"The best technology is the one that gets out of your way.\" ## Get in Touch If you'd like to connect or discuss anything, feel free to reach out through [my GitHub](https://github.com/bestjosephine). I'm always happy to chat about interesting ideas. Thanks for visiting, and stay tuned for more posts!"
  }
];
